__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.env
.DS_Store
.cache/

# Decision log (app/memory/) - this instance's own track record, and the
# only thing this app writes to disk. Machine-local by design: it records
# what *this* deployment concluded, so committing it would mix one
# machine's history into everyone's checkout.
data/
